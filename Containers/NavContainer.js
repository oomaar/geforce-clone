import { TopNav, BottomNav } from "../Components"

const NavContainer = () => {
    return (
        <>
            <TopNav />
            <BottomNav />
        </>
    );
};

export default NavContainer;
